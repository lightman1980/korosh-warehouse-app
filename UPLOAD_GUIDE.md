# 📤 راهنمای آپلود فایل‌ها به سرور

## ✅ فایل‌هایی که باید آپلود کنید:

### 🔴 **فایل ضروری (حتماً باید آپلود شود):**

1. **`.htaccess`** - این فایل **خیلی مهم** است و باید حتماً آپلود شود
   - مسیر: `dist/.htaccess`
   - این فایل برای رفع خطای 403 Forbidden ضروری است

### 🟢 **فایل‌های اصلی برنامه:**

2. **`index.html`** - فایل اصلی HTML
3. **فولدر `assets/`** - شامل تمام فایل‌های CSS و JavaScript
4. **فولدر `images/`** - شامل تصاویر برنامه
5. **`vite.svg`** - آیکون برنامه

---

## 📋 مراحل آپلود:

### مرحله 1: Build کردن برنامه
```bash
npm run build
```

### مرحله 2: بررسی فایل‌های dist
بعد از build، فولدر `dist` باید شامل این فایل‌ها باشد:
```
dist/
├── .htaccess          ← این فایل خیلی مهم است!
├── index.html
├── vite.svg
├── assets/
│   ├── index-*.css
│   └── index-*.js
└── images/
    └── koorosh-logo.png
```

### مرحله 3: آپلود به سرور
**تمام محتویات** فولدر `dist` را به **root directory** سرور آپلود کنید:
- اگر از cPanel استفاده می‌کنید: فولدر `public_html`
- اگر از FTP استفاده می‌کنید: root directory سرور

### مرحله 4: تنظیم مجوزهای فایل‌ها
- فایل‌ها: `644` (rw-r--r--)
- فولدرها: `755` (rwxr-xr-x)

---

## ⚠️ نکات مهم:

1. **فایل `.htaccess` حتماً باید آپلود شود** - بدون این فایل، خطای 403 خواهید دید
2. **همه فایل‌ها باید در root directory باشند** - نه در subdirectory
3. **ساختار فولدرها را حفظ کنید** - فولدر `assets` و `images` باید به همان صورت باشند

---

## 🔧 اگر خطای 403 دیدید:

1. مطمئن شوید فایل `.htaccess` آپلود شده است
2. مجوزهای فایل‌ها را بررسی کنید (644 برای فایل‌ها، 755 برای فولدرها)
3. با پشتیبانی هاستینگ تماس بگیرید و بپرسید:
   - آیا `mod_rewrite` فعال است؟
   - آیا فایل `.htaccess` اجرا می‌شود؟

---

## 📝 یادداشت:

- فایل `.htaccess` به صورت خودکار از فولدر `public` به `dist` کپی می‌شود
- بعد از هر build، فایل `.htaccess` در فولدر `dist` موجود خواهد بود
- نیازی به کپی دستی نیست - Vite این کار را انجام می‌دهد

