# راهنمای رفع مشکل تغییر وضعیت تراکنش‌ها

## مشکل اصلی شناسایی شد ✅

مشکل در **تعریف نوع TypeScript** بود. نوع `status` در فایل `WarehouseDeliveryTypes.ts` شامل `'issued'` نبود و فقط `'draft'` داشت. این مشکل اکنون **برطرف شده** است.

---

## مراحل رفع مشکل

### مرحله 1: بررسی تغییرات اعمال شده

تغییرات زیر در کد اعمال شده‌اند:

1. ✅ **اضافه شدن `'issued'` به نوع status** در `WarehouseDeliveryTypes.ts`
2. ✅ **فیلدهای اضافی** مانند `isVoided`, `slipDate`, `event`, `_editingSource` به نوع اضافه شدند
3. ✅ **دکمه "پاکسازی کش برنامه"** در سربرگ جدول اضافه شد
4. ✅ **دکمه "بررسی گردد"** برای هایلایت زرد ردیف‌ها اضافه شد
5. ✅ **دکمه "درخواست اصلاحیه"** با اعتبارسنجی کامل اضافه شد
6. ✅ **تغییر وضعیت** با تطبیق هم‌زمان بر اساس `id` و `transactionNumber`
7. ✅ **نمایش شماره تراکنش** به جای شماره مجوز در جدول

---

### مرحله 2: پاک کردن کش و مشاهده تغییرات

#### گزینه 1: استفاده از دکمه داخل برنامه (توصیه می‌شود)

1. برنامه را باز کنید
2. به بخش **"تراکنش‌های ثبت شده امانی/تملیکی"** بروید
3. در سربرگ این جدول، دکمه **"پاکسازی کش برنامه"** را مشاهده خواهید کرد
4. روی آن کلیک کنید
5. پیام تأیید نمایش داده می‌شود و صفحه خودکار رفرش می‌شود
6. ✅ داده‌های شما (localStorage) **حذف نمی‌شوند**

#### گزینه 2: استفاده از Developer Tools

1. `F12` را فشار دهید
2. به تب **Application** بروید
3. در منوی سمت چپ:
   - **Cache Storage** را باز کنید و همه را پاک کنید
   - **Service Workers** را باز کنید و همه را Unregister کنید
4. صفحه را رفرش کنید (`F5`)

#### گزینه 3: Hard Refresh

- ویندوز/لینوکس: `Ctrl + Shift + R`
- مک: `Cmd + Shift + R`

---

### مرحله 3: تست عملکرد با اسکریپت دیباگ

یک فایل به نام `debug-status-fix.js` در پوشه پروژه ایجاد شده است.

#### نحوه استفاده:

1. فایل `debug-status-fix.js` را با Notepad باز کنید
2. تمام محتوای آن را کپی کنید
3. `F12` را فشار دهید و به تب **Console** بروید
4. محتوا را Paste کنید و `Enter` بزنید

#### دستورات موجود:

```javascript
// نمایش وضعیت همه تراکنش‌ها
checkAllStatuses()

// ریست همه تراکنش‌ها به "پیش‌نویس"
resetAllToDraft()

// تغییر وضعیت یک تراکنش خاص
toggleStatusById('delivery_1731234567890')
toggleStatusById('WHD-2024-001')

// نمایش فقط تراکنش‌های "صادر شده"
showIssuedTransactions()

// همگام‌سازی همه لیست‌ها
syncAllLists()

// پاک کردن کش (بدون حذف داده‌ها)
clearCacheOnly()
```

---

### مرحله 4: تست عملکرد در رابط کاربری

پس از پاک کردن کش، این موارد را تست کنید:

#### 1. ایجاد تراکنش جدید
- تراکنش جدید ایجاد کنید
- بعد از ذخیره، در جدول باید **کادر قرمز** داشته باشد
- وضعیت باید **"پیش‌نویس"** باشد

#### 2. ویرایش تراکنش
- روی دکمه **"ویرایش"** کلیک کنید
- بلافاصله باید **کادر قرمز** ظاهر شود و وضعیت به **"پیش‌نویس"** تغییر کند

#### 3. تغییر وضعیت
- روی دکمه **"تغییر وضعیت"** (آیکن چرخشی سبز/نارنجی) کلیک کنید
- وضعیت باید بین **"پیش‌نویس"** و **"صادر شده"** جابه‌جا شود
- **کادر قرمز** باید حذف/اضافه شود

#### 4. بررسی گردد (هایلایت زرد)
- روی دکمه **"بررسی گردد"** (آیکن چک‌باکس زرد) کلیک کنید
- کل ردیف باید **زرد** شود
- با کلیک مجدد، رنگ زرد باید برداشته شود

#### 5. درخواست اصلاحیه
- اگر تراکنش در وضعیت **"صادر شده"** باشد:
  - دکمه باید **غیرفعال** باشد
  - با کلیک، پیام خطا نمایش داده شود
- اگر هیچ حواله‌ای برای مجوز نباشد:
  - درخواست اصلاحیه **ثبت** شود

---

## اگر هنوز کار نکرد

### 1. بررسی کنسول مرورگر
- `F12` را فشار دهید
- به تب **Console** بروید
- آیا خطای JavaScript وجود دارد؟
- اسکرین‌شات بگیرید و بررسی کنید

### 2. بررسی فایل‌های استفاده شده
- مطمئن شوید برنامه از این فایل استفاده می‌کند:
  ```
  src/components/WarehouseDelivery/WarehouseDeliveryManager.tsx
  ```
- **نه** از این فایل:
  ```
  src/components/WarehouseDelivery/Old/WarehouseDeliveryManager.tsx
  ```

### 3. بررسی Build
- اگر از Vite/React استفاده می‌کنید:
  ```bash
  npm run dev
  ```
- مطمئن شوید سرور در حال اجرا است

### 4. پاک کردن node_modules و نصب مجدد
```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

---

## خلاصه تغییرات اعمال شده

### فایل‌های تغییر یافته:

1. **src/types/WarehouseDeliveryTypes.ts**
   - اضافه شدن `'issued'` به نوع `status`
   - اضافه شدن فیلدهای `isVoided`, `slipDate`, `event`, etc.

2. **src/components/WarehouseDelivery/WarehouseDeliveryManager.tsx**
   - دکمه "پاکسازی کش برنامه" در سربرگ جدول
   - دکمه "بررسی گردد" برای هایلایت زرد
   - دکمه "درخواست اصلاحیه" با اعتبارسنجی
   - تغییر نمایش شماره تراکنش
   - بهبود منطق `handleChangeStatus`
   - بهبود منطق `handleEditTransaction`

### رفتارهای جدید:

1. **ایجاد/ذخیره تراکنش**: وضعیت همیشه "پیش‌نویس" است
2. **ویرایش تراکنش**: وضعیت بلافاصله به "پیش‌نویس" تغییر می‌کند
3. **تغییر وضعیت**: بین "پیش‌نویس" و "صادر شده" جابه‌جا می‌شود
4. **کادر قرمز**: تراکنش‌های "پیش‌نویس" کادر قرمز دارند
5. **هایلایت زرد**: با "بررسی گردد" کل ردیف زرد می‌شود

---

## پشتیبانی

اگر پس از اجرای این مراحل هنوز مشکل دارید:

1. اسکرین‌شات از Console را بگیرید
2. خروجی `checkAllStatuses()` را ارسال کنید
3. مشخص کنید کدام قسمت کار نمی‌کند:
   - ایجاد تراکنش جدید؟
   - ویرایش تراکنش؟
   - تغییر وضعیت؟
   - درخواست اصلاحیه؟
   - بررسی گردد؟

---

**تاریخ ایجاد**: 2024
**نسخه**: 1.0

